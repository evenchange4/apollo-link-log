// @flow

// gen via ./node_modules/.bin/flow gen-flow-files src/index.js > src/index.js.flow

import chalk from 'chalk';
import { print } from 'graphql/language';
import { ApolloLink } from 'apollo-link';
declare export function DEFAULT_LOGGER(_: {
  operation: Object,
  operationName: ?string,
  query: string,
  variables: Object,
}): void;
export type LoggerParams = {
  operation: Object,
  operationName: ?string,
  query: string,
  variables: Object,
};
export type Options = {
  enabled?: boolean,
  logger?: (params: {
    operation: Object,
    operationName: ?string,
    query: string,
    variables: Object,
  }) => void,
};
declare export default function a(_: {
  enabled?: boolean,
  logger?: (params: {
    operation: Object,
    operationName: ?string,
    query: string,
    variables: Object,
  }) => void,
}): any;
